spring:
  application:
    name: ai-ticket-classifier
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.0
server:
  port: 8080
